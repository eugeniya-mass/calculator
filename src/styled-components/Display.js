import styled from 'styled-components';

export const Display = styled.div`
  border-radius: .5rem;
  font-size: 3rem;
  padding: 24px 24px;
  text-align: right;
  text-overflow: ellipsis;
  overflow: hidden;
`
